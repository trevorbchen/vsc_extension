def verify_code(annotated_code: str) -> dict:
    # TODO: Replace with actual API call to verifier
    # Should return { "verified": bool, "errors": [str] }
    return {
        "verified": False,
        "errors": ["Example error: Function foo may not terminate."]
    }
